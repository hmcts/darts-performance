plugins {
    id 'java'
    id 'io.gatling.gradle' version '3.10.4'
}

gatling {
    logLevel = 'WARN'
    logHttp = 'NONE'
}

repositories {
    mavenCentral()
}
dependencies {

    gatling group: 'org.postgresql', name: 'postgresql', version: '42.7.3'

}